# Core dependencies
pydantic>=2.0.0
pydantic-ai>=0.1.0

# Async HTTP
aiohttp>=3.9.0
aiofiles>=23.0.0

# RSS parsing
feedparser>=6.0.0

# SSL certificates
certifi>=2023.0.0

# Google Gemini (for researcher agent)
google-genai>=1.0.0

# OpenAI client (for local MLX server compatibility)
openai>=1.0.0

# Local MLX inference (macOS Apple Silicon only)
# Requires macOS 15.0+
mlx-lm>=0.20.0; sys_platform == "darwin"

# Embeddings for hybrid RAG (BM25 + Vector search)
sentence-transformers>=2.2.0
numpy>=1.24.0

# Observability (optional)
logfire>=0.50.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
